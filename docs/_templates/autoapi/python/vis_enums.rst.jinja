
{% block vis_enums %}
{% set visible_enums = visible_classes|selectattr("obj.bases", "contains", "enum")|list %}

{% if visible_enums %} 
Enums
-----

.. toctree::
   :hidden:

      {% for enum in visible_enums %}
   {{ enum.include_path }}
      {% endfor %}


.. autoapisummary::

      {% for enum in visible_enums %}
   {{ enum.id }}
      {% endfor %}

{% endif %} 
{% endblock vis_enums %}
